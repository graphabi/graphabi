# Draft: X

```text
GraphABI alpha.3 is on PyPI:

pip install graphabi==0.1.0a3

It tests explicit, trace-observable semantic contracts across agent-system changes and reports the
first breaking edge plus downstream impact.

LangGraph and OpenAI Agents SDK adapters included. Alpha, local, no telemetry.

https://github.com/graphabi/graphabi
```
