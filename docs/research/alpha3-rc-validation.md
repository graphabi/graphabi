# Alpha 3 release-candidate validation suite

Status: engineering validation record, not a release decision. Performed against main commit
`33222d2` after merging `docs: clarify LangGraph list-parent fan-in` (#41) and
`fix: close leaked SQLite connections; add local Ollama provider quick start` (#42). Every item
below is a real command run on this machine or a real CI job, not a projection.

| Area | Result | Evidence |
|---|---|---|
| Unit / integration / property tests | PASS | `make test`: 228 passed, 91.41% coverage (floor 90%), Python 3.12.13 |
| Same suite, Python 3.13 | PASS | `uv run --python 3.13 pytest --cov=graphabi ...`: 228 passed, 91.41% coverage; only 1 residual warning (unrelated asyncio event-loop test fixture), down from 142 before the SQLite connection-leak fix |
| Clean Python 3.12 install | PASS | `uv pip install --python <isolated venv 3.12> dist/graphabi-0.1.0a2-py3-none-any.whl`; `graphabi doctor` exit 0, all required checks PASS |
| Clean Python 3.13 install | PASS | Same, `--python 3.13`; `graphabi doctor` exit 0 |
| Wheel smoke test | PASS | `uv run --python 3.12 --isolated --with dist/graphabi-0.1.0a2-py3-none-any.whl graphabi demo --allow-breaking`: exit 0, expected semantic FAIL, offline report written. Also exercised in CI as the `build-and-install` job on both merged PRs. |
| Sdist smoke test | PASS | `uv run --python 3.12 --isolated --with dist/graphabi-0.1.0a2.tar.gz graphabi doctor`: builds from source and installs; exit 0 |
| `graphabi doctor` | PASS | Run from clean wheel and sdist installs on 3.12 and 3.13; all required checks PASS, optional checks correctly INFO |
| `graphabi demo` | PASS | Deterministic breaking proof reproduced identically across the repo checkout, the wheel install, and the sdist install |
| Contract coverage | PASS | `graphabi demo` output: 3/3 graph edges contracted and observed, 100.0% observed contract coverage, explicit "Coverage is not correctness" statement present |
| Trace schema migration | PASS | `tests/unit/test_traces_storage.py::test_trace_v1_upgrade_assigns_singleton_causal_occurrences`, `test_sqlite_migrates_trace_v01_edge_primary_key` (part of the 228) |
| Report schema migration | PASS | Report 0.1/0.2 acceptance covered in `tests/adversarial/test_inputs_storage_reporting.py` and `tests/unit/test_traces_storage.py` (part of the 228) |
| LangGraph example | PASS | `graphabi demo` (real `research_graph` LangGraph execution) plus `tests/integration/test_langgraph_occurrences.py`, `tests/integration/test_research_graph.py` |
| OpenAI Agents SDK example | PASS | `uv run --python 3.12 --isolated --with dist/graphabi-0.1.0a2-py3-none-any.whl[openai-agents] python -m examples.openai_agents_adapter.example`: "Run status: success", 2 node occurrences, 1 handoff edge, 0 network calls |
| Local Ollama provider example | PASS, real inference | `uv run python -m examples.local_provider_quickstart.example --live` against a locally running Ollama 0.32.14 with `qwen3:8b`: `Structural compatibility: PASS`, `Semantic compatibility: FAIL`, reason `verified=true had no opened supporting source recorded for this run`, matching the production reality sprint's SAFE/BREAKING result exactly. Fixture (default, no network) mode also verified. |
| OTLP import example | PASS | `tests/end_to_end/test_cli.py::test_import_otel_command` exercises the real `import-otel` CLI path end to end (success, unsupported-span, and invalid-document cases); `tests/unit/test_telemetry_import.py` covers the mapping profile in depth (part of the 228) |
| GitHub Action self-test | PASS | CI `action-self-test` job green on both merged PRs (#41, #42), most recently run `32173280136` |
| Identity continuity regression | PASS | `tests/integration/test_langgraph_occurrences.py` (includes the new uneven fan-in list-parent and fail-closed cases from #41) |
| Authority UNKNOWN behavior | PASS | `tests/unit/test_correctness_sprint.py` (multiple `UNKNOWN` assertions for undeclared/unknown authority labels); regression corpus `authority` case |
| Complex occurrence example | PASS | Fan-out/fan-in and uneven-branch join tests in `tests/integration/test_langgraph_occurrences.py`; `regression_corpus` `fan-out` case (`PASS (baseline PASS, MATCH)`) |
| Offline report | PASS | `.graphabi/reports/latest/{report.json,index.html}` generated by every demo/doctor run above; self-contained HTML, no network requests |
| Security hygiene | PASS | 25-test adversarial storage/reporting/redaction suite passes (part of the 228); no secret-pattern matches in tracked files; local-only demo and doctor confirmed to make no network call except the explicit `--live` opt-in path |
| Privacy hygiene | PASS after one round of fixes | A `git ls-files` scan for the author's account name, absolute macOS user-home paths, and Gmail addresses found only `.github/CODEOWNERS` (a public GitHub handle, correct as-is). A second, broader pass for home-relative shell shorthand found two real leaks predating this validation pass, in `docs/research/alpha3-hostile-validation.md` and (introduced during this RC prep, before being caught here) `docs/research/alpha3-rc-gap-analysis.md` and `docs/releases/v0.1.0-alpha.3.md`: all referenced a personal `graphabi-lab` checkout path instead of the project's own established convention (used elsewhere in the same hostile-validation doc) of naming the separate `graphabi-lab` directory without a path prefix. All four were corrected to match that convention. This file itself was then found, by the project's own `test_tracked_text_has_no_private_absolute_paths` CI check, to contain the literal path prefix it was describing, and was rewritten to describe it without reproducing it. |
| No em dash characters | PASS | Repository-wide scan for the em dash character (U+2014) across `.md`/`.py`/`.yml`/`.toml`: zero matches as prose punctuation |
| No personal paths | PASS after fix | See privacy hygiene above: `~/Developer/...` prefixes found and removed from four locations across three files. |
| No Gmail identities | PASS | Same scan; zero matches |
| No unsupported claims | PASS | Repository-wide scan for absolute-claim language (`guarantee`, `100% accurate`, `certified`, `proven safe`, `zero false`, `enterprise-grade`, etc.): the one match is a negation ("not... capacity guarantees") in `docs/limitations.md` |

## Notes

- The wheel and sdist installs above are separate from, and in addition to, the CI
  `release-dry-run.yml` workflow's `build-and-install` job, which runs the same class of check
  automatically on every pull request and was green on both #41 and #42.
- The local Ollama provider live run used models already pulled during the earlier production
  reality sprint (`qwen3:8b`, `qwen3:4b`); no new external download was required. `ollama serve`
  was started locally for this validation pass.
- No BLOCKER was found anywhere in this pass. This validation suite, combined with
  `docs/research/alpha3-rc-gap-analysis.md`, is the evidence basis for the Alpha.3 recommendation.
